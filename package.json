{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Chat App is a real-time application similar to Slack-chat (very simplified version).",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npx start-server -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm ci --prefix frontend"
  },
  "author": "Dmitriy Lyubimov",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^1.1.6"
  }
}
